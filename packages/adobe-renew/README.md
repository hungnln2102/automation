# Adobe Renew Reference

This package is a reference area only. The active runtime code is now part of the main workspace:

- Backend: `apps/backend/src/services/renew-adobe`
- Frontend: `apps/frontend/src/features/renew-adobe`

Root dev commands are owned by `Automation/package.json`, not this folder.
