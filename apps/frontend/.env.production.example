# Sao chép thành `.env.production` trước khi build production (file `.env.production` không commit).
# API trên subdomain — khớp DNS A `api.otp90.com` → cùng VPS, Nginx: deploy/nginx/otp90-automation.conf

VITE_API_BASE_URL=https://api.otp90.com
